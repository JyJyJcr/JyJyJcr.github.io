---
import type { FooterLocalization } from "./Footer.types";

interface Props {
  loc: FooterLocalization;
}

const { loc } = Astro.props;
//const commitHash = String(import.meta.env.PUBLIC_COMMIT_HASH ?? "dev");
---

<footer>
  <p>{loc.builtWithAstro}</p>
</footer>
