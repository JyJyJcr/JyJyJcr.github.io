import type { AstroUserConfig } from "astro";
import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  site: "https://jyjyjcr.github.io",
  base: "/",
}) satisfies AstroUserConfig;
